# Generated from orogen/lib/orogen/templates/tasks/CMakeLists.txt

include(camera_prosilicaTaskLib)
ADD_LIBRARY(${CAMERA_PROSILICA_TASKLIB_NAME} SHARED 
    ${CAMERA_PROSILICA_TASKLIB_SOURCES})

add_dependencies(${CAMERA_PROSILICA_TASKLIB_NAME}
    regen-typekit)


TARGET_LINK_LIBRARIES(${CAMERA_PROSILICA_TASKLIB_NAME}
    ${OrocosRTT_LIBRARIES}
    ${CAMERA_PROSILICA_TASKLIB_DEPENDENT_LIBRARIES})
SET_TARGET_PROPERTIES(${CAMERA_PROSILICA_TASKLIB_NAME}
    PROPERTIES LINK_INTERFACE_LIBRARIES "${CAMERA_PROSILICA_TASKLIB_INTERFACE_LIBRARIES}")

INSTALL(TARGETS ${CAMERA_PROSILICA_TASKLIB_NAME}
    RUNTIME DESTINATION bin
    LIBRARY DESTINATION lib/orocos)

INSTALL(FILES ${CAMERA_PROSILICA_TASKLIB_HEADERS}
    DESTINATION include/orocos/camera_prosilica)

